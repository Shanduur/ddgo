kind: pipeline
type: docker
name: build

platform:
  os: linux
  arch: arm64

steps:
- name: publish
  image: plugins/docker
  privileged: true
  settings:
    repo: shanduur/ddgo
    auto_tag: true
    username:
      from_secret: docker_username
    password:
      from_secret: docker_password

